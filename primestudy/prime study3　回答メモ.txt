prime study3　回答メモ

④：v=3, w=4になる。x=17, y=13.0, z=3.0
⑩：elseは、for文にかかっても、if文にかかってもインデントの位置がおかしいのでナシ。break:という構文もないのでナシ。★aの後にbreakを入れたら3の時に終了するのでナシ。passは何もないと一緒。
⑯：結果はa、b、cの値でソートされている。このようなソートにする場合、Key引数には関数を渡す必要があり、関数の返り値によってソートするメソッドである。
今回は各要素の文字の辞書順でソートしたいので、各要素のインデックス番号1の値を返す関数を定義すればよい。
いちいちdefで関数を定義するのは面倒なので、無名関数lambdaを使用。
。
⑯2・3：lambdaの引数argの位置に[0][1]が入っているのはダメ