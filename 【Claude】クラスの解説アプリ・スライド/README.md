# Pythonクラス学習教材セット

このセットは、Pythonのクラスの概念を成績表アプリを通して学ぶための教材です。

## 📦 含まれるファイル

### 1. `student_grade_app.py`
**Tkinter成績表アプリ**

生徒の成績を管理するGUIアプリケーションです。`Student`クラスを使用して、クラスの基本的な概念を実践的に学べます。

#### 機能
- 生徒の氏名、国語・数学・英語の点数を入力
- 自動的に合計点と平均点を計算
- 成績一覧をテーブル形式で表示
- 入力バリデーション付き

#### 使い方
```bash
python student_grade_app.py
```

#### 学習ポイント
- `Student`クラスの定義と使用
- `__init__`メソッド（コンストラクタ）
- インスタンス属性（`self.name`, `self.japanese`など）
- インスタンスメソッド（`calculate_total()`, `calculate_average()`）
- クラスのインスタンス化
- オブジェクト指向の基本概念

### 2. `python_class_tutorial.pptx`
**解説スライドショー（全7枚）**

アプリと連動した理論解説スライドです。

#### スライド構成
1. **タイトル** - Pythonのクラス入門
2. **クラスとは？** - 設計図としての概念とコード例
3. **インスタンス** - 設計図から実体を作る
4. **属性とメソッド** - データと機能の違い
5. **Studentクラスの設計** - 実際のクラス定義
6. **実際の使用例** - コードと実行結果
7. **まとめ** - 重要ポイントの振り返り

## 🎯 学習の進め方

### ステップ1: スライドで理論を学ぶ
PowerPointスライドを開いて、クラスの基本概念を理解します。

### ステップ2: アプリを動かす
Tkinterアプリを実行して、実際にクラスがどう動作するか確認します。

### ステップ3: コードを読む
`student_grade_app.py`のコードを開いて、以下の部分を確認：

#### Studentクラスの定義（22行目〜）
```python
class Student:
    """生徒クラス - 生徒の情報と成績を管理"""
    
    def __init__(self, name, japanese, math, english):
        self.name = name
        self.japanese = japanese
        self.math = math
        self.english = english
```

#### メソッドの定義（46行目〜）
```python
def calculate_total(self):
    """合計点を計算するメソッド"""
    return self.japanese + self.math + self.english

def calculate_average(self):
    """平均点を計算するメソッド"""
    return self.calculate_total() / 3
```

#### インスタンスの作成（278行目〜）
```python
# Studentクラスのインスタンスを作成
student = Student(name, japanese, math, english)
self.students.append(student)
```

### ステップ4: カスタマイズしてみる
理解が深まったら、以下のような拡張に挑戦：

- **新しいメソッドを追加**
  - `get_grade()`：平均点から評価（A, B, Cなど）を返すメソッド
  - `is_passing()`：合格判定メソッド

- **新しい属性を追加**
  - 理科や社会の科目を追加
  - 学年や出席番号を追加

- **UIの改善**
  - 削除ボタンを追加
  - 編集機能を追加
  - グラフ表示機能を追加

## 💡 クラスの重要概念

### クラス（設計図）
```python
class Student:
    # クラスの定義
```
「生徒」という概念の設計図。何度でも使い回せる。

### インスタンス（実体）
```python
student1 = Student("太郎", 85, 90, 78)
student2 = Student("花子", 92, 88, 95)
```
設計図から作った実際のデータ。それぞれ独立している。

### 属性（データ）
```python
self.name = name
self.japanese = japanese
```
オブジェクトが持っているデータ。

### メソッド（機能）
```python
def calculate_total(self):
    return self.japanese + self.math + self.english
```
オブジェクトができる処理。

## 🔧 必要な環境

- Python 3.x
- Tkinter（通常Pythonに標準で含まれています）

## 📝 よくある質問

### Q: `self`って何？
A: インスタンス自身を指す特別な変数です。メソッド内で自分の属性にアクセスするときに使います。

### Q: `__init__`って何？
A: コンストラクタと呼ばれる特殊なメソッドで、インスタンスが作られるときに自動的に呼ばれます。

### Q: クラスを使う利点は？
A: 関連するデータと処理をまとめて管理できるので、コードが整理され、再利用しやすくなります。

## 📚 さらに学ぶには

- 継承：既存のクラスを拡張する
- カプセル化：データの隠蔽
- ポリモーフィズム：同じメソッド名で異なる動作
- クラス変数：全インスタンスで共有されるデータ
- クラスメソッドと静的メソッド

## 👨‍🏫 授業での活用例

1. **導入（15分）**
   - スライド1-4で理論を説明
   - クラスの必要性を議論

2. **実践（20分）**
   - アプリを実演
   - コードを一緒に読む
   - 実際に動かしてみる

3. **演習（25分）**
   - 生徒が自分でアプリを動かす
   - 新しいメソッドを追加する課題
   - ペアプログラミングで拡張機能を作成

---

**作成者**: Python学習教材
**目的**: Pythonのクラスを実践的に学ぶ
**対象**: Python初級〜中級者、オブジェクト指向プログラミング入門
